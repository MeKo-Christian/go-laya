# Pinned reference environment for the parity harness. THIS FILE IS THE CONTRACT:
# the tokenizer and transformers versions determine the golden vectors in testdata/,
# so changing a pin here means regenerating them as a reviewed diff (PLAN.md R6).
#
# Recreate with:
#   uv venv --python 3.12 .venv-ref
#   uv pip install --python .venv-ref/bin/python --index-strategy unsafe-best-match \
#       -r scripts/requirements-ref.txt
#
# torch is the CPU build, which does not live on PyPI -- hence the extra index below.
# --index-strategy is needed with it and cannot be set from this file: uv's default is
# first-index-wins, which finds `torch` on PyPI and then refuses the +cpu local version.
# It has to go on the command line.
--extra-index-url https://download.pytorch.org/whl/cpu

annotated-doc==0.0.5
anyio==4.15.1
certifi==2026.7.22
click==8.5.0
filelock==3.32.3
flatbuffers==25.12.19
fsspec==2026.7.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface-hub==1.32.0
idna==3.20
jinja2==3.1.6
markdown-it-py==4.2.0
markupsafe==3.0.3
mdurl==0.1.2
ml-dtypes==0.6.0
mpmath==1.3.0
networkx==3.6.1
numpy==2.5.3
onnx==1.23.0
onnx-ir==1.0.0
onnxruntime==1.30.0
onnxscript==0.7.2
packaging==26.3
protobuf==7.36.2
pygments==2.21.0
pyyaml==6.0.3
regex==2026.9.10
rich==15.0.0
safetensors==0.8.0
setuptools==78.1.0
shellingham==1.5.4
sympy==1.14.0
tokenizers==0.23.2
torch==2.14.0+cpu
tqdm==4.70.1
transformers==5.17.0
typer==0.27.2
typing-extensions==4.16.0
